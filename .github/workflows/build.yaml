name: Build and Release NinjaForge

permissions:
  contents: write

on:
  push:
    branches:
      - main
  release:
    types: [created]  # Trigger on new releases (e.g., when you tag a version)

jobs:
  build:
    runs-on: windows-latest  # WPF requires Windows for native dependencies

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'  # Adjust to your project's .NET version (e.g., 6.0.x or 7.0.x)

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore --configuration Release

    - name: Publish
      run: dotnet publish NinjaForge.csproj --no-build --configuration Release --output ./publish
      # If your project has a specific .csproj, add: -p:PublishSingleFile=true --self-contained true for a single EXE (optional, but user-friendly)

    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
          name: NinjaForge-${{ github.ref_name }}-publish   # or just "publish-output"
          path: ./publish/**   # uploads all files in folder; GitHub zips them once
          retention-days: 7  # Artifacts expire after 7 days; use releases for permanent

    - name: Create Release and Upload Asset
      if: github.event_name == 'release'  # Only run this step on releases
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ github.event.release.upload_url }}
        asset_path: NinjaForge-${{ github.ref_name }}.zip
        asset_name: NinjaForge-${{ github.ref_name }}.zip

        asset_content_type: application/zip
